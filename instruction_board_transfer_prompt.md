# 指示・発信ボード機能 実装依頼プロンプト

以下の要件を、既存システムの構成・フレームワーク・認証方式に合わせて実装してください。既存の無関係な画面や機能は変更しないでください。実装後は、画面表示と主要な操作をテストし、エラーがあれば修正して完成状態にしてください。

## 目的

管理者が住民向けの指示・発信を登録し、一覧で確認し、各発信の詳細を読み取り専用で確認できる機能を追加する。

## 実装対象画面

1. 指示・発信ボード画面
2. 新規登録画面
3. 指示詳細画面（編集不可）

## データ仕様

指示データは、既存システムの保存方式に合わせて永続化する。最低限、次の項目を保持する。

```json
{
  "id": 1,
  "target": "住民",
  "title": "指示タイトル",
  "content": "指示内容",
  "priority": "低",
  "region": "全地域",
  "age_groups": ["20代未満を含む"],
  "shelter": "",
  "status": "発信中",
  "created_at": "2026年09月17日 10:00",
  "updated_at": "2026年09月17日 10:00"
}
```

- `target` が `住民` のデータだけを指示・発信ボードに表示する。
- `priority` は `低`、`中`、`高` のいずれか。
- `region` の選択肢は `全地域`、`北部`、`南部`。
- `age_groups` の選択肢は `20代未満を含む`、`20〜50代`、`60代以上`。
- 新規登録時の `status` は `発信中` とする。
- 指示の解除後は `status` を `解除` に変更し、`updated_at` も更新する。
- JSON保存の場合は日本語をそのまま保存し、既存データを壊さない。

## 指示・発信ボード画面

画面タイトルは「指示・発信ボード」とし、説明文として「住民向けの発信を登録・確認できます。」を表示する。

上部に「新規登録」ボタンを配置する。押すと新規登録画面へ遷移する。

一覧表の列は次の5列とする。

| 列 | 内容 |
|---|---|
| 通知日時 | `created_at` |
| 重要度 | `priority` |
| 内容 | `content` と避難先情報、解除済み表示 |
| 詳細設定 | 詳細画面へのリンク「…」 |
| 通知操作 | 「解除通知を送信」ボタン |

### 内容欄

- 内容欄の各項目全体をクリック可能にする。
- クリックすると、その指示IDの詳細画面へ遷移する。
- 解除済みの場合は内容の末尾に `【解除済み】` と表示する。
- `shelter` がある場合は内容の後ろに `（避難先: 避難所名）` と表示する。
- 内容リンクは通常時に本文と同系色で表示し、ホバー時は下線を表示する。

### 重要度の表示

- `高`：淡い赤系背景と赤系文字
- `中`：淡い黄色系背景と濃い黄色系文字
- `低`：淡いグレー系背景
- `priority` が未設定の場合は、内容に「土砂災害」または「危険」が含まれていれば `高`、それ以外は `低` とする。

### 解除通知

- 「解除通知を送信」ボタンを押すと、対象の指示だけを解除する。
- 解除処理は対象IDを使ったPOST処理にする。
- 対象データの `status` を `解除` に更新し、保存する。
- 更新後は指示・発信ボードへリダイレクトする。
- 対象IDが存在しない場合もエラー画面にせず、ボードへ戻す。
- ボタンはフォームや表全体のカード装飾に影響されないようにする。

### ボードのデザイン

既存ホーム画面のカードデザインに合わせる。

- 白色または半透明の白背景
- 薄い境界線
- 角丸は約18px、モバイルでは約14px
- 控えめな影
- 表の見出し行はテーマ青 `#2457a6`、文字は白
- 表の区切り線は薄い `#edf1f5`
- 表は横幅が狭い画面で横スクロール可能
- 表の最低幅は約760px
- 通知操作列は約150px
- ボタンの角丸は約7px

## 指示詳細画面

URLは既存ルーティング規則に合わせ、指示IDを含む形式にする。例：`/instruction/1`。

- 認証済みユーザーだけが閲覧できる。
- 対象は `target == 住民` の指示だけに限定する。
- 存在しないIDの場合はボード画面へ戻す。
- 次の項目を読み取り専用で表示する。
  - 指示タイトル
  - 指示内容
  - 重要度
  - 対象地域
  - 対象年齢
  - 状態
- input、textarea、select、編集ボタンは置かない。
- 詳細画面にはボードへ戻るリンクを置く。
- 指示内容は改行を維持し、長い文字列がレイアウトからはみ出さないようにする。

## 新規登録画面

画面タイトルは「新規登録」とする。登録項目は次の順番にする。

1. 指示タイトル
2. 指示内容
3. 重要度
4. 対象地域
5. 対象年齢
6. 戻る、送信

### 入力仕様

すべての項目を入力・選択しないと送信できないようにする。

- 指示タイトル：必須のテキスト入力。自由入力。
- 指示内容：必須の複数行入力。自由入力。
- 重要度：`低`、`中`、`高` のラジオボタン。必須。
- 対象地域：未選択状態を含むセレクト。`全地域`、`北部`、`南部` から必須選択。
- 対象年齢：`20代未満を含む`、`20〜50代`、`60代以上` のチェックボックス。1つ以上必須。

対象年齢は複数選択を許可する。ただし、1つも選択されていない場合はブラウザ側で送信を止め、エラーを表示する。サーバー側でも同じ必須チェックを実施する。

サーバー側では次をすべて検証する。

- タイトルが空でないこと
- 内容が空でないこと
- 重要度が許可された3値のいずれかであること
- 対象地域が許可された3値のいずれかであること
- 対象年齢が1つ以上選択されていること

エラー時は入力済みの値をフォームに戻し、次の趣旨のエラーメッセージを表示する。

> タイトル、内容、重要度、対象地域、対象年齢をすべて入力してください。

登録成功時は保存後、指示・発信ボードへ遷移する。

### 新規登録画面のデザイン

既存ホーム画面と同じ雰囲気の白いカードを使う。

- 見出しと白い登録枠の間に約16pxの空白を置く。
- 登録枠は白背景、薄い境界線、約18pxの角丸、控えめな影。
- 入力欄とセレクトは幅100%、約7pxの角丸。
- 戻るボタンと送信ボタンの角丸は約7px。
- モバイルでは項目ラベルと入力欄を縦方向に並べる。

### フォントと文字色

新規登録画面内の文字は次のルールで統一する。

- フォント指定は `inherit` を使用する。
- 文字色は `#202b35` に統一する。
- 対象は、画面見出し、項目ラベル、重要度の選択肢、対象年齢の選択肢、入力欄、プレースホルダー、戻るボタン、送信ボタン、エラーメッセージ。
- 背景色、境界線、フォーカス枠などのUI装飾色は、文字色とは別に既存デザインへ合わせて設定してよい。

## 認証・既存構成との統合

- 既存システムに認証機能がある場合、ボード、詳細、登録、解除処理は認証必須にする。
- 既存のテンプレート継承、共通レイアウト、CSS、ルーティング方式を優先して使う。
- 既存のホーム画面のデザイン変数や共通スタイルがある場合は再利用する。
- 無関係な避難所機能やホームの気象・地図機能は変更しない。

## 完了条件

次のすべてを満たすこと。

- 指示・発信ボードに住民向け指示が表形式で表示される。
- 「新規登録」から登録画面へ遷移できる。
- 全入力項目が埋まっていない状態では送信できない。
- 対象地域が `全地域・北部・南部` になっている。
- 対象年齢が1つ以上必須になっている。
- 登録した指示がボードに表示される。
- ボードの内容欄をクリックすると詳細画面を開ける。
- 詳細画面で内容を確認できるが編集はできない。
- 解除通知を押すと対象の内容だけが解除状態になる。
- 解除後、内容末尾に `【解除済み】` が表示される。
- データが永続化され、ページ再読み込み後も状態が維持される。
- 存在しない詳細IDや解除IDでサーバーエラーにならない。
- デスクトップとモバイルの両方で崩れない。
- 既存機能に回帰がない。

## 実装後に必ず実施する確認

最低限、次の動作を確認する。

1. 認証済みでボードを開く。
2. 新規登録画面を開き、空のまま送信できないことを確認する。
3. 対象年齢を未選択のまま送信できないことを確認する。
4. 全項目を入力して登録できることを確認する。
5. 登録した内容がボードに表示されることを確認する。
6. 内容欄をクリックして読み取り専用詳細画面を開く。
7. 詳細画面に編集用フォームがないことを確認する。
8. ボードへ戻り、解除通知を送信する。
9. 対象内容だけに `【解除済み】` が表示されることを確認する。
10. ページを再読み込みし、解除状態が保存されていることを確認する。
11. テンプレート診断、構文チェック、テストを実行する。
